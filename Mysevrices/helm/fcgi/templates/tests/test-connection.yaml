apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fcgi.fullname" . }}-test-connection"
  labels:
    {{- include "fcgi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fcgi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
